<div class='row'>
  <div class='col-sm-8'>
    <div id='createWord' class='well'>
      <h2>Add words and define them.</h2>
      <form action='/output' method='post'>
        <div class='form-group'>
          <label for='name'>Word:</label>
          <input id='name' name='name' class='form-control' type='text'>
        </div>
        <div class='form-group'>
          <label for='definition'>Definition:</label>
          <input id='definition' name='definition' class='form-control' type='text'>
        </div>
        <button type='submit' class='btn'>Add!</button>
      </form>
    </div>
  </div>

  <div class='col-sm-4'>
    <div id='displayWords' class='well'>
      <h2>Words:</h2>
      <p>Click to see definitions...</p>
      <% @words.each do |word| %>
        <h4><a href='/output/<%= word.name %>'><%= word.name %></a></h4>
      <% end %>
    </div>
  </div>
</div>
